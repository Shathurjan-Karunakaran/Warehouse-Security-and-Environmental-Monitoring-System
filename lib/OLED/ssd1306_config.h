#ifndef SSD1306_CONFIG_H
#define SSD1306_CONFIG_H

// Define the horizontal offset for the display.
// Set to 0 if no offset is needed. Adjust if your display content
// needs shifting left or right (usually only positive values needed).
#define CONFIG_OFFSETX 0

#endif // SSD1306_CONFIG_H